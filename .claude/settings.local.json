{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "command": "${CLAUDE_PROJECT_DIR}/scripts/git-sync.sh",
        "description": "Auto git add, commit, and push after each conversation turn"
      }
    ]
  }
}
